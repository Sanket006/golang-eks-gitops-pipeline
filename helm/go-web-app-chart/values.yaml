# Values for go-web-app-chart.
# Only the image.tag value is used at runtime — it is automatically
# updated by the GitHub Actions CI/CD pipeline on every push to main.

image:
  repository: sanket006/go-web-app
  pullPolicy: IfNotPresent
  # Auto-updated by GitHub Actions (update-newtag-in-helm-chart job)
  tag: "27408635955"
